#!/bin/bash

# Sets up the tasksync tool so that it can be executed from any location.

# Copy tasksync to /usr/local/bin/
sudo cp task/tasksync /usr/local/bin

# Make tasksync executable
sudo chmod +x /usr/local/bin/tasksync

